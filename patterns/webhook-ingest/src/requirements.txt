# The consumer uses only the standard library and the AWS-provided boto3 that is
# already present in the Lambda runtime, so there are no extra packages to bundle.
# Pin any downstream clients you add inside process_event here.
